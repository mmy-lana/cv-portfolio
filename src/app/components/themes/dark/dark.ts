import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-dark',
  imports: [RouterLink],
  templateUrl: './dark.html',
  styles: ``,
  standalone: true,
})
export class Dark { }
